name: Build & Publish Documentation
on:
  push:
    branches:
      - API-26886-test-techdocs
  workflow_dispatch:

jobs:
  publish-documentation:
    runs-on:
      - bandicoot-self-hosted-dev
      - techdocs
    uses: department-of-veterans-affairs/lighthouse-developer-portal/.github/workflows/build-publish-docs.yaml@API-26886-update-techdocs
    with:
      enable-production: false
      